[mypy]

# psutil has no typeshed stubs compatible with Python 2 (its stub package
# dropped Python 2 support before mypy's own --py2 checking was removed),
# so it can never be resolved here. Ignore missing imports for just this
# module rather than globally.
[mypy-psutil]
ignore_missing_imports = True
